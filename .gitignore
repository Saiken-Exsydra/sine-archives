# Local Netlify folder
.netlify
node_modules/
dist/
.astro/
.DS_Store
.env
# Decap admin local styling (ignore unless intentionally customizing)
public/admin/admin.css
public/admin/preview.css

# Deno lockfile (ignore unless using Deno intentionally)
deno.lock

