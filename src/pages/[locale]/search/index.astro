---
import SearchPage from "../../search/index.astro";

export const prerender = true;

export function getStaticPaths() {
  return [{ params: { locale: "pt-br" } }];
}
---

<SearchPage locale="pt-br" />
